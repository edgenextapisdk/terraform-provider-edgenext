# EdgeNext RDS Services

Terraform data sources for EdgeNext RDS OpenAPI.

## Data sources

- `edgenext_rds_instances` — `DataSourceENRDSInstances` (`data_source_en_rds_instances.go`): list RDS instances via `/rds/openapi/v2/instances/list`.
